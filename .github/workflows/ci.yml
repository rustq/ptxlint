name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - run: cargo fmt --check
      - run: cargo clippy --all-targets -- -D warnings
      - run: cargo test --release

  # Dogfooding: this runner has no GPU and no CUDA toolkit, which is exactly
  # the situation ptxlint is built for.
  lint-ptx:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo build --release
      - name: Known-good kernels must pass
        run: |
          ./target/release/ptxlint --deny error \
            tests/fixtures/clean_saxpy.ptx \
            tests/fixtures/nanoid_fixed.ptx
      - name: The deliberately broken fixture must still be caught
        run: |
          if ./target/release/ptxlint --deny error tests/fixtures/local_array.ptx; then
            echo "expected ptxlint to fail on the broken fixture"
            exit 1
          fi

  # The .ptx fixtures are generated from Rust; make sure that still compiles.
  build-kernels:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@nightly
        with:
          targets: nvptx64-nvidia-cuda
          components: llvm-bitcode-linker, llvm-tools
      - run: cargo build --release --examples
        working-directory: fixtures
